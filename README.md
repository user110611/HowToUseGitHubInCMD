# 🐙 Как пользоваться GitHub через командную строку

## 📦 Установка

### 1. Установи Git
Скачай с официального сайта: https://git-scm.com/downloads  
После установки проверь:
```bash
git --version
```

### 2. Первоначальная настройка
```bash
git config --global user.name "Твоё Имя"
git config --global user.email "твой@email.com"
```

---

## 🔑 Авторизация на GitHub

### Через Personal Access Token (рекомендуется)
1. Зайди на GitHub → Settings → Developer settings → Personal access tokens → Generate new token
2. При первом `git push` введи логин и вместо пароля вставь токен

### Через SSH (удобнее для постоянной работы)
```bash
# Сгенерируй SSH-ключ
ssh-keygen -t ed25519 -C "твой@email.com"

# Скопируй публичный ключ
cat ~/.ssh/id_ed25519.pub

# Вставь его на GitHub → Settings → SSH and GPG keys → New SSH key
```

---

## 🚀 Основные команды

### Клонировать репозиторий
```bash
git clone https://github.com/username/repo-name.git
cd repo-name
```

### Создать новый репозиторий локально
```bash
mkdir my-project
cd my-project
git init
```

### Подключить к GitHub
```bash
git remote add origin https://github.com/username/repo-name.git
```

---

## 📝 Ежедневный workflow

```bash
# 1. Посмотреть статус файлов
git status

# 2. Добавить файлы в коммит
git add .                  # все файлы
git add index.html         # конкретный файл

# 3. Сделать коммит
git commit -m "Описание изменений"

# 4. Отправить на GitHub
git push origin main

# 5. Получить изменения с GitHub
git pull origin main
```

---

## 🌿 Работа с ветками

```bash
# Создать новую ветку и переключиться на неё
git checkout -b feature/my-feature

# Посмотреть все ветки
git branch

# Переключиться на другую ветку
git checkout main

# Слить ветку в main
git merge feature/my-feature

# Удалить ветку
git branch -d feature/my-feature

# Отправить ветку на GitHub
git push origin feature/my-feature
```

---

## 🔍 Просмотр истории

```bash
# История коммитов
git log

# Краткая история (одна строка)
git log --oneline

# Что изменилось в файлах
git diff
```

---

## ↩️ Отмена изменений

```bash
# Отменить изменения в файле (до git add)
git checkout -- filename.txt

# Убрать файл из git add
git reset HEAD filename.txt

# Отменить последний коммит (сохранив изменения)
git reset --soft HEAD~1

# Отменить последний коммит (удалив изменения) ⚠️
git reset --hard HEAD~1
```

---

## 💡 Полезные советы

| Ситуация | Команда |
|---|---|
| Посмотреть удалённые репозитории | `git remote -v` |
| Обновить список веток с GitHub | `git fetch` |
| Сохранить изменения временно | `git stash` |
| Вернуть сохранённые изменения | `git stash pop` |
| Удалить файл из репозитория | `git rm filename.txt` |

---

## 🗺️ Типичный процесс работы над проектом

```
1. git clone <url>          → скачать проект
2. git checkout -b fix/bug  → создать ветку
3. ... вносишь изменения ...
4. git add .                → подготовить файлы
5. git commit -m "fix: ..."  → сохранить коммит
6. git push origin fix/bug  → отправить на GitHub
7. Открыть Pull Request на GitHub
8. После merge: git checkout main && git pull
```
